return {
  "numToStr/Comment.nvim",
  opts = {
    --   -- Normal mode
    --   toggler = {
    --     -- Should disable that, doesn't work on react code, should use "gc" instead
    --     line = "<leader>/",
    --     -- block = "<leader>/",
    --   },
    --   -- Normal and visual mode
    --   opleader = {
    --     line = "<leader>/",
    --     -- block = "<leader>/",
    --   },
  },
  -- lazy = false,
}
